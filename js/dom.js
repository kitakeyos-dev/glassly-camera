// DOM element references and offscreen canvases

const videoEl      = document.querySelector('.input_video');
const canvasEl     = document.querySelector('.output_canvas');
const ctx          = canvasEl.getContext('2d');
const loadingEl    = document.getElementById('loading');
const loadingTitleEl = document.getElementById('loading-title');
const loadingSubtitleEl = document.getElementById('loading-subtitle');
const loadingProgressFillEl = document.getElementById('loading-progress-fill');
const errorEl      = document.getElementById('error-screen');
const helpBtnEl    = document.getElementById('help-btn');
const helpSheetEl  = document.getElementById('help-sheet');
const helpCloseBtnEl = document.getElementById('help-close-btn');
const historyShortcutBtnEl = document.getElementById('history-shortcut-btn');
const gestureTagEl = document.getElementById('gesture-tag');
const saveToastEl  = document.getElementById('save-toast');
const countdownOverlayEl = document.getElementById('countdown-overlay');
const filterBarEl = document.getElementById('filter-bar');
const paletteBarEl = document.getElementById('palette-bar');
const timerToggleEl = document.getElementById('timer-toggle');
const timerToggleLabelEl = document.getElementById('timer-toggle-label');
const switchCameraBtnEl = document.getElementById('switch-camera-btn');
const fullscreenBtnEl = document.getElementById('fullscreen-btn');
const captureBtnEl = document.getElementById('capture-btn');
const captureBtnImgEl = captureBtnEl.querySelector('img');
const overlayBackdropEl = document.getElementById('overlay-backdrop');
const clearFrameBtnEl = document.getElementById('clear-frame-btn');
const insertPhotoBtnEl = document.getElementById('insert-photo-btn');
const glassEditorEl = document.getElementById('glass-editor');
const glassEditorCanvasEl = document.getElementById('glass-editor-canvas');
const glassEditorCtx = glassEditorCanvasEl.getContext('2d');
const glassEditorCloseBtnEl = document.getElementById('glass-editor-close-btn');
const glassEditorSaveBtnEl = document.getElementById('glass-editor-save-btn');
const glassEditorPickerListEl = document.getElementById('glass-editor-picker-list');
const glassEditorUploadBtnEl = document.getElementById('glass-editor-upload-btn');
const glassEditorUploadInputEl = document.getElementById('glass-editor-upload-input');
const effectsBtnEl = document.getElementById('effects-btn');
const effectsSheetEl = document.getElementById('effects-sheet');
const beautyToggleEl = document.getElementById('beauty-toggle');
const beautyStrengthEl = document.getElementById('beauty-strength');
const beautyStrengthValueEl = document.getElementById('beauty-strength-value');
const skinWhitenToggleEl = document.getElementById('skin-whiten-toggle');
const skinWhitenStrengthEl = document.getElementById('skin-whiten-strength');
const skinWhitenStrengthValueEl = document.getElementById('skin-whiten-strength-value');
const historyDrawerEl = document.getElementById('history-drawer');
const historyCloseBtnEl = document.getElementById('history-close-btn');
const historyEmptyEl = document.getElementById('history-empty');
const historyListEl = document.getElementById('history-list');
const historyPreviewEl = document.getElementById('history-preview');
const historyPreviewImageEl = document.getElementById('history-preview-image');
const historyPreviewTitleEl = document.getElementById('history-preview-title');
const historyPreviewTimeEl = document.getElementById('history-preview-time');
const historySelectionCountEl = document.getElementById('history-selection-count');
const historyUploadBtnEl = document.getElementById('history-upload-btn');
const uploadHistoryInputEl = document.getElementById('upload-history-input');
const uploadCustomStickerInputEl = document.getElementById('upload-custom-sticker-input');
const stickerUploadBtnEl = document.getElementById('sticker-upload-btn');
const historyDownloadBtnEl = document.getElementById('history-download-btn');
const historyDeleteBtnEl = document.getElementById('history-delete-btn');
const historyShareBtnEl = document.getElementById('history-share-btn');
const historyEditBtnEl = document.getElementById('history-edit-btn');
const historyClearSelectionBtnEl = document.getElementById('history-clear-selection-btn');
const editorOverlayEl = document.getElementById('editor-overlay');
const editorCanvasEl = document.getElementById('editor-canvas');
const editorCtx = editorCanvasEl.getContext('2d');
const editorDownloadBtnEl = document.getElementById('editor-download-btn');
const editorCloseBtnEl = document.getElementById('editor-close-btn');
const editorUndoBtnEl = document.getElementById('editor-undo-btn');
const editorRedoBtnEl = document.getElementById('editor-redo-btn');
const editorLayoutOptionsEl = document.getElementById('editor-layout-options');
const editorPhotoPickerEl = document.getElementById('editor-photo-picker');
const editorFrameOptionsEl = document.getElementById('editor-frame-options');
const stickerSearchInputEl = document.getElementById('sticker-search-input');
const stickerListEl = document.getElementById('sticker-list');
const textInputEl = document.getElementById('text-input');
const textFontSelectEl = document.getElementById('text-font-select');
const textSizeRangeEl = document.getElementById('text-size-range');
const textSizeValueEl = document.getElementById('text-size-value');
const textColorRowEl = document.getElementById('text-color-row');
const textAddBtnEl = document.getElementById('text-add-btn');
const editorSelectionNoteEl = document.getElementById('editor-selection-note');
const editorTabbarEl = document.querySelector('.editor-tabbar');
const editorTabButtons = editorTabbarEl ? Array.from(editorTabbarEl.querySelectorAll('.editor-tab')) : [];
const editorBottomSheetEl = document.getElementById('editor-bottom-sheet');
const bottomSheetTitleEl = editorBottomSheetEl ? editorBottomSheetEl.querySelector('.bottom-sheet-title') : null;
const bottomSheetCloseBtnEl = editorBottomSheetEl ? editorBottomSheetEl.querySelector('.bottom-sheet-close') : null;
const editorTabPanels = editorBottomSheetEl ? Array.from(editorBottomSheetEl.querySelectorAll('.editor-tab-panel')) : [];
const stickerToolbarEl = document.getElementById('editor-sticker-toolbar');

const frozenCanvas = document.createElement('canvas');
const frozenCtx    = frozenCanvas.getContext('2d');
const editorExportCanvas = document.createElement('canvas');
editorExportCanvas.width = editorCanvasEl.width;
editorExportCanvas.height = editorCanvasEl.height;
const editorExportCtx = editorExportCanvas.getContext('2d');
